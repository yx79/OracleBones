<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.yuanjiexie.spring2016.oraclebones">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:label="Numbers 数字 （shù zì）"
            android:name=".NumbersActivity" />
        <activity
            android:label="Nature 自然 （zì rán）"
            android:name=".NatureActivity" />
        <activity
            android:label="Animals 动物 （dòng wù）"
            android:name=".AnimalsActivity" />
        <activity
            android:label="Life 生活 （shēng huó）"
            android:name=".LifeActivity"></activity>
    </application>

</manifest>